// Static dissolve removed — requires simultaneous access to both frames (two-buffer).
// File kept so Vite's glob pattern stays consistent.
// The TransitionRegistry skips modules with no default export.
export {};
